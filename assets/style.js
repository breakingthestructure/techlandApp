'use strict';

let React = require('react-native');

let {
    StyleSheet,
} = React;

module.exports = StyleSheet.create({
    alwaysred: {
        backgroundColor: 'red',
        height: 100,
        width: 100,
    },
});
